import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Navbar from '@/components/Navbar'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Montgomery Blair Go Tournament - Saturday, March 21st, 2026',
  description: 'Register for the Montgomery Blair Go Tournament on Saturday, March 21st, 2026. Join us at Montgomery Blair High School for competitive Go matches. Time: 9:30 AM - 5:00 PM',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="flex min-h-screen flex-col">
          <Navbar />
          <main className="flex-1">{children}</main>
          <Footer />
        </div>
      </body>
    </html>
  )
}
